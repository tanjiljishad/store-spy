/** The marketing-intelligence subset of EventType — shared by every UI surface that filters the change feed to advertising activity only. */
export const MARKETING_EVENT_TYPES = ["AD_DETECTED", "AD_REMOVED", "AD_CHANGED", "PRODUCT_AD_MATCHED"] as const;
